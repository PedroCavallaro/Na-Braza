export type AuthTypeObj = {
    login: AuthConst;
    register: AuthConst;
};

export type AuthConst = {
    title: string;
    buttonText: string;
};

export type Product = {
    name: string;
};
export type SlideType = {
    activeIndex: number;
};
